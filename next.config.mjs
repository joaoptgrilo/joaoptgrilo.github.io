// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options go here.
  // We can leave it empty for now or add standard ones:
  reactStrictMode: true,

  // Add other config options as needed later, for example:
  // output: 'export', // Needed for true static export to gh-pages (build step only)
  // images: {         // Needed if using next/image with static export
  //   unoptimized: true,
  // },
};

export default nextConfig;
